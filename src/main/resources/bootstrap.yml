spring:
  cloud:
    vault:
      authentication: TOKEN
      token: s.9TIJeTjLtfncwdZXNBHpoB8S
      scheme: http
      host: localhost
      port: 8200
      kv:
        enabled: true
        backend: secret
        profile-separator: '/'
        default-context: application
        application-name: vault-demo
